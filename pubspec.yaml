name: famcare_app
description: "AI Powered Family Health Tracker"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # ✅ DATABASE & AUTH
  supabase_flutter: ^2.5.6
  flutter_dotenv: ^5.1.0

  # 🔔 ALARMS & NOTIFICATIONS
  flutter_local_notifications: ^17.1.2
  timezone: ^0.9.2
  android_alarm_manager_plus: ^5.0.0

  # 📸 CAMERA & IMAGE
  image_picker: ^1.1.2
  path_provider: ^2.1.4

  # 🤖 AI & OCR (NEW)
  google_mlkit_text_recognition: ^0.13.0
  http: ^1.2.2
  intl: ^0.19.0

  # 🎨 UI & FONTS
  google_fonts: ^6.3.3
  lucide_icons: ^0.257.0
  audioplayers: ^6.6.0
  fl_chart: ^1.2.0
  shimmer: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/alarm.mp3
    - .env